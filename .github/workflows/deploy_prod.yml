name: Deploy Production

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-24.04-arm

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Capture target image version
        id: target
        run: |
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: .ruby-version
          bundler-cache: true

      - name: Install Kamal
        run: gem install kamal

      - name: Set up SSH
        run: |
          echo "${{ secrets.EC2_SSH_KEY }}" > claria-production.pem
          chmod 600 claria-production.pem

      - name: Deploy prebuilt image
        env:
          KAMAL_REGISTRY_PASSWORD: ${{ secrets.KAMAL_REGISTRY_PASSWORD }}
          ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY: ${{ secrets.ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY }}
          ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT: ${{ secrets.ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT }}
          ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY: ${{ secrets.ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY }}
          CLARIA_DATABASE_HOST: ${{ secrets.CLARIA_DATABASE_HOST }}
          CLARIA_DATABASE_PASSWORD: ${{ secrets.CLARIA_DATABASE_PASSWORD }}
          CLARIA_DATABASE_USER: ${{ secrets.CLARIA_DATABASE_USER }}
          SECRET_KEY_BASE: ${{ secrets.SECRET_KEY_BASE }}
          RAILS_MASTER_KEY: ${{ secrets.RAILS_MASTER_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          APP_RELEASE: ${{ steps.target.outputs.sha }}
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          kamal deploy --skip-push --version "${{ steps.target.outputs.sha }}"
          kamal prune all -v

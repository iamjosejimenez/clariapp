<div class="max-w-xl mx-auto p-6 bg-white rounded-2xl shadow space-y-6 mt-6">
  <h1 class="text-2xl font-semibold text-gray-800">Agregar gasto a "<%= @budget.name %>"</h1>
  <div>
    <%= render Expenses::ExpenseFormComponent.new(budget: @budget, budget_period: @budget_period, expense: @expense) %>
  </div>
</div>
